#include <stdio.h>
int main() {    
  printf("Addition deux nombres");
    int number1=1, number2=2, sum;
 
    sum = number1 + number2;      
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}
